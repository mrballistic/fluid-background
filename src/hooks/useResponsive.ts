import { UseResponsiveReturn } from '../types';

// Placeholder hook - will be implemented in task 7.2
const useResponsive = (): UseResponsiveReturn => {
  return {
    dimensions: { width: 0, height: 0 },
    devicePixelRatio: 1,
  };
};

export default useResponsive;